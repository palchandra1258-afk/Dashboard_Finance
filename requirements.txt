# Financial Dashboard Requirements
# Compatible with Python 3.9+ (Streamlit Cloud uses 3.9)
# Also works on Python 3.14+ locally

# Data manipulation
pandas>=2.0.0
openpyxl>=3.0.0
numpy>=1.24.0

# Statistical analysis
scipy>=1.10.0

# Visualization
plotly>=5.0.0
matplotlib>=3.5.0
seaborn>=0.12.0

# Web framework
streamlit>=1.28.0

# NOTE: PyArrow is intentionally NOT included
# This dashboard uses HTML table rendering (df.to_html()) 
# instead of st.dataframe() or st.table() to avoid PyArrow dependency
# This ensures broad Python version compatibility (3.9-3.14+)
